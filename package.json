{
  "name": "burak",
  "version": "1.0.0",
  "description": "This is backend of Burak Project",
  "main": "src/server.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "ts-node src/server.ts",
    "build": "tsc",
    "start:dev": "nodemon --exec ts-node src/server.ts",
    "train": "nodemon --exec python3 train.py"
  },
  "author": "MIT",
  "license": "ISC",
  "dependencies": {
    "moment": "^2.30.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2",
    "ts-node": "^10.9.1",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.2.2"
  }
}